import React from 'react';
import withDashboard from '../../../shared/DashboardLayout/DashboardLayout';

const FlightPackage = () => {
    return (
      <div>
        <h1>FlightPackage</h1>
      </div>
    );
};

export default withDashboard(FlightPackage);